---
title: "The Phone Cloud for Agents"
sidebarTitle: "Home"
description: "Real Android phones on carrier networks, controlled by a vision-native Python SDK. Build reliable mobile agents that run at scale."
mode: "wide"
---

Genuine Android phones on live carrier networks — allocated on demand, driven by a vision-native SDK, observable at every step. No emulators, no scripted browsers, no mocked networks.

```python
from axilio.platform import Client
from axilio.drivers import MobileDriver

client = Client()

with client.mobile.session(platform="android") as device:
    driver = MobileDriver(device)
    driver.find_text("Sign in").tap()
    driver.find(query="the blue Continue button").tap()
```

<Columns className="responsive-columns" cols={2}>
  <Card title="Real hardware" img="/images/real-hardware.svg" href="/concepts/devices">
    Physical Android handsets, not virtual machines. Real sensors, OEM behavior, and carrier features all work as expected.
  </Card>
  <Card title="Live carrier networks" img="/images/carrier-network.svg" href="/introduction">
    Genuine SIMs on real networks. SMS OTPs, voice calls, and carrier-gated flows work without any mocking layer.
  </Card>
  <Card title="Vision-native control" img="/images/vision-control.svg" href="/concepts/mobile-driver">
    Argus locates elements from plain-English queries or fast OCR. Describe what you see; the driver finds it.
  </Card>
  <Card title="Parallel at scale" img="/images/parallel.svg" href="/guides/parallel-execution">
    Fan out across thousands of isolated device sessions at once, each billed per second and released automatically.
  </Card>
</Columns>

<Card title="Quickstart — zero to a live phone in 5 minutes" icon="rocket" iconType="sharp-solid" href="/quickstart" horizontal>
  Install the SDK, set your API key, and drive a real Android device end to end. The fastest way to understand what Axilio does.
</Card>

## Start here

<Columns className="responsive-columns" cols={3}>
  <Card title="Quickstart" icon="rocket" iconType="sharp-solid" href="/quickstart">
    Install the SDK, set your key, and drive your first device in minutes.
  </Card>
  <Card title="Authentication" icon="key" iconType="sharp-solid" href="/authentication">
    Create API keys and pass credentials securely.
  </Card>
  <Card title="MobileDriver" icon="mobile" iconType="sharp-solid" href="/concepts/mobile-driver">
    Find elements, tap, type, and read any Android screen.
  </Card>
</Columns>

## Core concepts

<Columns className="responsive-columns" cols={2}>
  <Card title="Devices & sessions" icon="mobile" iconType="sharp-solid" href="/concepts/devices">
    Allocate real phones from the fleet, drive them, release them. Billed per second.
  </Card>
  <Card title="Workflows & runs" icon="diagram-project" iconType="sharp-solid" href="/concepts/workflows-and-runs">
    Package automation as reusable workflows and trigger them via the API.
  </Card>
  <Card title="Argus vision" icon="eye" iconType="sharp-solid" href="/api-reference/argus/overview">
    OCR, object detection, and VLM element locating on any image.
  </Card>
  <Card title="Sandbox mode" icon="box" iconType="sharp-solid" href="/concepts/sandbox-mode">
    Develop against a deterministic device emulator before touching the fleet.
  </Card>
</Columns>

<Note>
  Axilio is in **early access**. The SDK surface is still expanding and APIs may change between minor versions before 1.0. Pin a specific version for reproducible builds, and watch the [changelog](/reference/changelog) for breaking changes.
</Note>
