---
title: "The Phone Cloud for Agents"
sidebarTitle: "Home"
description: "Real Android phones on carrier networks, driven by a vision-native Python SDK. Allocate a device, drive it with text and vision selectors, and ship mobile agents to production."
mode: "wide"
---

Axilio gives your agents **real Android phones in the cloud** — and a simple Python
library to control them. Pick up a phone, read its screen, and tap, type, and
swipe, just like a person would.

Most apps can't be plugged into directly — they have no public API. So an agent
uses them the same way you do: on a real phone. That's what Axilio is for.

<CardGroup cols={4}>
  <Card title="Connect" icon="plug" href="/quickstart">
    Authenticate, select a provisioned phone, and start a phone control session.
  </Card>
  <Card title="Control" icon="hand-pointer" href="/guides/search-x-with-the-interactive-cli">
    Observe the screen, find elements by text or meaning, then tap, type, and swipe.
  </Card>
  <Card title="Automate" icon="bolt" href="/workflows/overview">
    Convert live interactions to repeatable workflows. Launch workflow runs.
  </Card>
  <Card title="o11y" icon="eye" href="/observability/overview">
    Inspect status, events, logs, enabled recordings, telemetry, and cost.
  </Card>
</CardGroup>

## Give your AI a phone

Copy this prompt into your coding agent. The prompt gives it a safe setup path,
a first phone test, and the references it needs to build your automation.

```text
Help me set up Axilio, prove that you can drive a real phone, and then use it to
implement my mobile automation requests. Take ownership of the setup, but pause
for my permission whenever you need to create credentials or change my account.

1. Install the current Axilio CLI from an official source:
   - macOS with Homebrew: brew install axilioai/tap/axilio
   - macOS or Linux: curl -fsSL https://axilio.ai/install.sh | sh
   Verify the install with `axilio --version`, then run `axilio doctor`.

2. Configure yourself to use Axilio. Run `axilio init` in my project, read the
   instruction file it creates, and follow it. Before guessing at a command,
   consult `axilio --help`, `axilio <command> --help`, and `man axilio`. You can
   also run `axilio help --html` for the installed browser reference.

3. Help me authenticate with an API key. Ask me whether I want to create one
   myself at https://app.axilio.ai/api-keys?create=1 or authorize you to use an
   already signed-in browser to create one for me. If you have browser-control
   tools, offer to perform that browser flow, but wait for my explicit approval
   before creating the key. Never ask me to paste a key into chat or expose it
   in logs. Have me enter it directly in my terminal and store it with the CLI,
   or set `AXILIO_API_KEY` outside the conversation. Confirm authentication by
   rerunning `axilio doctor`.

4. Run one small, harmless test on a newly allocated phone:
   - Start and pin a session with `eval "$(axilio sessions start --export)"`.
   - Run `axilio phone observe -o json`.
   - From that observation, choose a harmless visible target such as the
     Settings app. Interact with it using a semantic command such as
     `axilio phone tap --query "the Settings app"`, then observe again to verify
     the screen changed. Do not use raw coordinates when a semantic target is
     available.
   - Always release the phone with
     `axilio sessions stop "$AXILIO_SESSION" --yes`, even if the test fails.

5. Use these sources as your reference material:
   - CLI source and install guide: https://github.com/axilioai/cli
   - Axilio docs: https://docs.axilio.ai
   - Interactive CLI guide:
     https://docs.axilio.ai/guides/search-x-with-the-interactive-cli
   - REST API reference: https://docs.axilio.ai/api-reference/overview
   - Driver reference: https://docs.axilio.ai/driver/overview
   - Workflows: https://docs.axilio.ai/workflows/overview

Once the test succeeds, summarize what you verified and ask what mobile task I
want to implement. Explore the task one action at a time with the CLI, observe
after each action, and always release the phone. Then give me a durable Python
or Go SDK workflow that reproduces the successful interaction without requiring
you to remain in the loop.
```

## What you get

<CardGroup cols={3}>
  <Card title="Real hardware" icon="microchip">
    Real consumer Android phones — the same devices people use every day, not
    emulators or virtual devices.
  </Card>
  <Card title="Carrier networks" icon="signal">
    Real SIMs on real carrier networks — genuine mobile connectivity, not a
    datacenter.
  </Card>
  <Card title="Vision-native" icon="eye">
    Find any element with natural language, backed by vision models — not brittle
    coordinate scripts.
  </Card>
  <Card title="Parallel execution" icon="bolt">
    Run concurrent, isolated sessions with automatic load balancing across the
    fleet.
  </Card>
  <Card title="Predictable pricing" icon="credit-card">
    Per-second billing of device time. No data fees, no proxy fees, no setup fees.
  </Card>
  <Card title="Replay & traces" icon="film">
    Full session replay and per-step traces to debug agents in production.
  </Card>
</CardGroup>

## Keep going

<CardGroup cols={3}>
  <Card title="Workflows & runs" icon="diagram-project" href="/workflows/overview">
    Package automation as a reusable workflow and run it on demand.
  </Card>
  <Card title="Observability" icon="video" href="/observability/overview">
    Recordings, events, and cost for every session.
  </Card>
  <Card title="API Reference" icon="code" href="/api-reference/overview">
    Every REST endpoint, generated from the live spec.
  </Card>
</CardGroup>
