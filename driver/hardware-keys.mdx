---
title: "Press a device button"
sidebarTitle: "Device buttons"
description: "Press the buttons that aren't on the screen — Home, Back, volume, and media keys."
---

Some buttons aren't on the screen — Home, Back, volume. Press them with
`key_press` and the `Key` names.

```python
from axilio.platform import Client
from axilio.drivers.mobile import Key

client = Client()

with client.session("ANDROID") as driver:
    driver.key_press(Key.HOME)   # go to the home screen
```

## The buttons

| Group | Names |
|---|---|
| Navigation | `Key.HOME`, `Key.BACK`, `Key.RECENTS`, `Key.MENU` |
| Power | `Key.POWER` |
| Volume | `Key.VOLUME_UP`, `Key.VOLUME_DOWN`, `Key.MUTE` |
| Media | `Key.PLAY_PAUSE`, `Key.NEXT_TRACK`, `Key.PREV_TRACK`, `Key.STOP` |
| System | `Key.SEARCH`, `Key.ASSIST` |

<Note>
  `key_press` is for device buttons. To type text, use
  [`type_text` or `type_into`](/driver/actions) instead.
</Note>

## Next steps

<CardGroup cols={2}>
  <Card title="Tap, type, and swipe" icon="hand-pointer" href="/driver/actions">
    Act on things on the screen.
  </Card>
  <Card title="Waiting" icon="hourglass-half" href="/driver/waiting">
    Wait for the screen after a button press.
  </Card>
</CardGroup>
