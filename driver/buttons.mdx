---
title: "Press a key"
sidebarTitle: "Keys"
description: "Press a named key like Enter to submit a form or fire the keyboard's Go / Search."
---

Some steps need a key press, not a tap — submitting a form, or firing the on-screen
keyboard's **Go** / **Search**. Use [`key_press`](/driver/reference#driver-key_press)
with a [`Key`](/driver/key) constant:

```python
from axilio.drivers.mobile import Key

driver.find_text("Search").type_into("axilio")
driver.key_press(Key.ENTER)   # submit
```

<Note>
  `Key.ENTER` is the only named key today; the set grows as real needs appear. To
  type characters into a field, use [Type](/driver/type) — not `key_press`.
</Note>

## Next steps

<CardGroup cols={2}>
  <Card title="Type" icon="keyboard" href="/driver/type">
    Type text into a field.
  </Card>
  <Card title="Key reference" icon="code" href="/driver/key">
    Every named-key constant.
  </Card>
</CardGroup>
