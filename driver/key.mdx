---
title: "Key"
sidebarTitle: "Key"
description: "The named-key constants you pass to driver.key_press(). Today, that's Enter."
---

`Key` holds the named-key constants for
[`driver.key_press()`](/driver/reference#driver-key_press).

<Tabs sync={false}>
  <Tab title="CLI">
    ```bash
    axilio phone key enter
    ```
  </Tab>

  <Tab title="Python SDK">
    ```python
    from axilio.drivers.mobile import Key

    driver.key_press(Key.ENTER)
    ```
  </Tab>

  <Tab title="Go SDK">
    ```go
    if err := driver.KeyPress(mobile.KeyEnter); err != nil {
        log.Fatal(err)
    }
    ```
  </Tab>
</Tabs>

This live phone action is not available through REST.

## Constants

| Constant | Value | What it does |
|---|---|---|
| `Key.ENTER` | `"enter"` | Submit a form, or fire the on-screen keyboard's Go / Search action. |

<Note>
  Enter is the only named key today. The set grows one entry at a time, as real
  needs appear. To type characters (rather than press a key), use
  [`driver.type_text()`](/driver/reference#driver-type_text) or
  [`element.type_into()`](/driver/element#element-type_into).
</Note>

## See also

<CardGroup cols={2}>
  <Card title="Driver" icon="mobile" href="/driver/reference">
    `key_press()` and every other action.
  </Card>
  <Card title="Keys" icon="keyboard" href="/driver/buttons">
    The guide to pressing a key.
  </Card>
</CardGroup>
